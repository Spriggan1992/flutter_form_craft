# flutter_form_craft example

Run from this directory:

```bash
flutter pub get
flutter run
```

This example demonstrates:
- `buildField`
- `setValues`
- `validateDetailed`
- `clearForm`
- phone mask with configurable `fixedPrefix`
